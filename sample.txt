이 코드에서 onCreate(content)는 즉시 실행되는 함수 호출입니다.
즉, 버튼이 클릭되지 않아도 컴포넌트가 렌더링될 때 바로 실행됩니다.
onClick 속성에 전달된 함수가 아니라, 함수의 결과값이 전달됩니다.
React는 onClick이벤트를 처리할 때 함수가 필요하지만, onCreate(content)처럼 괄호 ()를 붙이면 그 함수가 즉시 실행되어 반환된 결과값(보통 undefined)을 onClick에 넘깁니다. 그러면 클릭 이벤트 자체가 제대로 동작하지 않게 됩니다.